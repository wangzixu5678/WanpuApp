<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:fitsSystemWindows="true"
    android:clipToPadding="true"
    tools:context="com.example.administrator.wanpuapp.MainActivity">
    <FrameLayout
        android:id="@+id/main_container"
        android:layout_weight="1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>
    <RadioGroup
        android:id="@+id/main_radiogroup"
        android:layout_gravity="bottom"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <RadioButton
            android:id="@+id/main_home"
            style="@style/ButtonStyle"
            android:drawableTop="@drawable/home_white"
            android:text="首页"
            />
        <RadioButton
            android:id="@+id/main_message"
            style="@style/ButtonStyle"
            android:drawableTop="@drawable/message_white"
            android:text="咨讯"
            />
        <RadioButton
            android:id="@+id/main_resource"
            style="@style/ButtonStyle"
            android:drawableTop="@drawable/supply_white"
            android:text="供求"
            />
        <RadioButton
            android:id="@+id/main_product"
            style="@style/ButtonStyle"
            android:drawableTop="@drawable/product_white"
            android:text="产品"
            />
        <RadioButton
            android:id="@+id/main_user"
            style="@style/ButtonStyle"
            android:drawableTop="@drawable/message_white"
            android:text="我的"
            />
    </RadioGroup>
</LinearLayout>
